/**
 * Este archivo es un redireccionamiento al cliente único de Supabase.
 * IMPORTANTE: No debemos crear nuevas instancias de GoTrueClient aquí.
 */
export { default as supabase, supabase as default } from '@/lib/supabase'; 